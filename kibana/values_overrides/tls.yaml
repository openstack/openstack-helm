---
conf:
  kibana:
    elasticsearch:
      ssl:
        certificateAuthorities: ["/etc/elasticsearch/certs/ca.crt"]
        verificationMode: certificate
endpoints:
  elasticsearch:
    scheme:
      default: "https"
    port:
      http:
        default: 443
manifests:
  certificates: true
...
