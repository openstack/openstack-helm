---
# Reconcile the Vhost/User/Permission/Queue/Exchange/Binding/Policy custom
# resources rendered by the consumer charts. The definitions themselves are
# deployed by default; this turns on the controller that acts on them.
manifests:
  crds: true
  deployment_topology_controller: true
...
