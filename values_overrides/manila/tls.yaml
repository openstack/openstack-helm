---
endpoints:
  oslo_db:
    host_fqdn_override:
      default:
        tls:
          secretName: manila-mariadb-client
          issuerRef:
            name: ca-issuer
            kind: ClusterIssuer
          usages:
            - client auth
manifests:
  certificates: true
tls:
  oslo_db: true
...
