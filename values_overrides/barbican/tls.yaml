---
endpoints:
  oslo_db:
    host_fqdn_override:
      default:
        tls:
          secretName: barbican-mariadb-client
          issuerRef:
            name: ca-issuer
            kind: ClusterIssuer
          usages:
            - client auth
manifests:
  certificates: true
tls:
  identity: true
  oslo_messaging: true
  oslo_db: true
...
