---
# Reconcile the Database/User/Grant/Connection custom resources rendered by
# the consumer charts. The definitions themselves are deployed by default;
# this turns on the controller that acts on them.
manifests:
  crds: true
  deployment_db_controller: true
...
