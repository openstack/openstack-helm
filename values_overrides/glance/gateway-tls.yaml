# Gateway API + TLS overrides for Glance.
#
# Same as gateway.yaml but registers the public endpoint as https://...
---
endpoints:
  image:
    host_fqdn_override:
      public:
        host: glance.openstack-helm.org
    scheme:
      public: https
    port:
      api:
        public: 443

extraObjects:
  - apiVersion: gateway.networking.k8s.io/v1
    kind: HTTPRoute
    metadata:
      name: glance-route
      namespace: "{{ .Release.Namespace }}"
    spec:
      hostnames:
        - "{{ .Values.endpoints.image.host_fqdn_override.public.host }}"
      parentRefs:
        - name: gateway-default
          namespace: envoy-gateway-system
      rules:
        - matches:
            - path:
                type: PathPrefix
                value: /
          backendRefs:
            - name: glance-api
              port: 9292
...
