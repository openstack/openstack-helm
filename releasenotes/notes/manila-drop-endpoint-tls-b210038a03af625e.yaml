---
manila:
  - |
    Removed the unused ``endpoints.oslo_db.auth.admin.secret.tls`` metadata and
    the orphaned oslo_db client-certificate volume in the share statefulset;
    the shared ``client-certs`` volume (from ``secrets.tls.client.manila``) is
    used instead.
...
