---
ironic:
  - |
    Add an opt-in values override that runs Ironic Prometheus Exporter through
    ``conductor.extraContainers`` and shares a pod-local metrics directory
    through the existing conductor mount hooks. The selected conductor image
    must include the ``prometheus_exporter`` notification driver, exporter
    application, and uWSGI.
...
