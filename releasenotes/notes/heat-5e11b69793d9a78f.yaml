---
# To create a new release note related to a specific chart:
# reno new <chart_name>
#
# To create a new release note for a common change (when multiple charts
# are changed):
# reno new common
heat:
  - |
    Update heat-api and heat-cfn to use /healthcheck for readiness probe.
    Liveness probe is left pointing to uwsgi stats port.
...
