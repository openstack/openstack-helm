---
keystone:
  - |
    Allow enabling TLS for the Keystone API and for the Keystone to MariaDB
    connection independently via the tls.identity and tls.oslo_db values, and
    use a dedicated client certificate when connecting to MariaDB over TLS
    instead of reusing the MariaDB server certificate on the client side.
...
