---
placement:
  - |
    Rename the MariaDB client certificate secret from placement-mariadb-client to
    placement-client. It is now the chart's single client certificate for mutual TLS
    backend connections.
...
