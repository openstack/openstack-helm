---
keystone:
  - |
    Making keystone credential secrets completely dynamically created.
    We need to keep those secrets when we delete release, but those
    helm complains that they exist if we do `helm upgrade --install`.
    Now the setup jobs are responsible for those secrets lifecycle.
...
