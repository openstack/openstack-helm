---
heat:
  - |
    Replaced uWSGI ``wsgi-file`` with ``module`` directive for OpenStack
    2026.1 compatibility. Heat API now uses ``heat.wsgi.api:application``
    and Heat CFN API uses ``heat.wsgi.cfn:application``. The wsgi script
    entry points have been removed upstream in favor of direct module
    references.
...
