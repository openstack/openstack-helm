---
helm-toolkit:
  - |
    Gate the RabbitMQ client certificate mount and the REQUIRE X509 environment
    in the rabbit-init job manifest on the presence of a TLS secret (tlsSecret)
    instead of the manifests.certificates value, so messaging TLS can be
    toggled independently of certificate rendering.
...
