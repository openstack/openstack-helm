---
ovn:
  - |
    Adds a new DaemonSet template for ovn-bgp-agent, gated on
    `manifests.daemonset_ovn_bgp_agent`. The agent runs on nodes where both
    openvswitch and ovn-controller are deployed. The template includes a
    sidecar for frr, which is gated on `sidecars.frr` and defaults to false.
...
