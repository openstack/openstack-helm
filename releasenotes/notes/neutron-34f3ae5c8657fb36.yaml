---
neutron:
  - |
    Add the ``ovn-neutron-init`` container to the neutron ironic agent
    deployment and pass ``ovn.ini`` to the agent, so ``ovn_nb_connection`` is
    set rather than falling back to ``tcp:127.0.0.1:6641``.
...
