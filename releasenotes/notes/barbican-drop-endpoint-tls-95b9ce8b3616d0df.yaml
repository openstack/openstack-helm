---
barbican:
  - |
    Removed the unused ``endpoints.oslo_db.auth.admin.secret.tls`` and
    ``endpoints.oslo_messaging.auth.admin.secret.tls`` metadata. The client
    certificate secret is sourced from ``secrets.tls.client.<service>``.
...
