---
rabbitmq:
  - |
    Enforce mutual TLS on the AMQP listener by setting
    ssl_options.fail_if_no_peer_cert to true in the TLS value overrides. Clients
    must present a client certificate signed by the trusted CA to connect.
...
