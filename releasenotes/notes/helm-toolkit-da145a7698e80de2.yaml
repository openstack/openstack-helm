---
helm-toolkit:
  - |
    Gate the MariaDB client certificate mount and the REQUIRE X509 grant in the
    mysql db-init and db-drop job manifests on the tls.oslo_db value so database
    TLS can be toggled independently of the manifests.certificates value.
...
