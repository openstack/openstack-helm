---
prometheus-openstack-exporter:
  - |
    Removed the unused start script and its ConfigMap entry. The
    exporter binary is launched directly by the deployment and the
    script was never mounted or invoked.
...
