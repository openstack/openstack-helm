---
ceph-adapter-rook:
  - |
    Add a retry loop to ensure the Ceph client key is available before saving
    it to a secret. This prevents the Ceph adapter from failing if the key is
    not immediately available.
...
