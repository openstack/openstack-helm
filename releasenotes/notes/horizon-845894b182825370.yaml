---
horizon:
  - |
    Rename the MariaDB client certificate secret from horizon-mariadb-client to
    horizon-client. It is now the chart's single client certificate for mutual TLS
    backend connections.
...
