---
trove:
  - |
    The MariaDB and RabbitMQ client certificate is now issued as a dedicated
    per-chart certificate (``secrets.tls.client.trove``, minted by
    ``certificates.yaml`` when ``tls.oslo_db``/``tls.oslo_messaging`` is
    enabled) instead of reusing the server certificates via the removed
    ``endpoints.oslo_db.auth.admin.secret.tls`` and
    ``endpoints.oslo_messaging.auth.admin.secret.tls`` values.
...
