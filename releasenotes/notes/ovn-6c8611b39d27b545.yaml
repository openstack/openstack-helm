---
ovn:
  - |
    Fix the OVN liveness check to run as the less privileged user to match the
    permission characteristics, otherwise the check succeeds in cases where the
    daemon is in a failed state.
...
