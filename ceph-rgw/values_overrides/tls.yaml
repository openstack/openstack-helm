---
endpoints:
  object_store:
    host_fqdn_override:
      default:
        tls:
          secretName: ceph-rgw-ks-tls-api
          issuerRef:
            name: ca-clusterissuer
            kind: ClusterIssuer
  ceph_object_store:
    host_fqdn_override:
      default:
        tls:
          secretName: ceph-rgw-s3-tls-api
          issuerRef:
            name: ca-clusterissuer
            kind: ClusterIssuer

manifests:
  certificates: true
...
